# 노트북 설치 가이드 — 파이퍼 시연 데이터 수집 (UGRP)

> 목적: **노트북에서 파이퍼로 시연 데이터를 수집·저장**하기 위한 설치 순서.
> 대상 프로젝트: [DGIST-JaeminBaek/UGRP](https://github.com/DGIST-JaeminBaek/UGRP)
> (파이퍼용 LeRobot 플러그인 — 마스터-슬레이브 텔레오퍼레이션으로 시연 녹화)
>
> ⚠️ 이 가이드는 **노트북(로봇이 물리적으로 연결된 리눅스 PC)** 에서 실행합니다.
> 서버(컨테이너)에는 CAN·카메라가 없어 수집을 실행할 수 없습니다.

---

## 0. 사전 조건 (하드웨어 / OS)

- [ ] 파이퍼 로봇팔 **2대** (마스터 + 슬레이브) — CAN 어댑터 내장
- [ ] Intel RealSense **D435 계열 카메라 2대** (탑뷰 + 손목뷰)
- [ ] **Ubuntu 22.04** (repo 검증 버전) — SocketCAN 필수
      - 더 최신 우분투를 쓸 거면 conda/pip 버전 충돌만 주의
- [ ] 노트북 여유 공간 **20~30 GB** 권장 (환경 ~5~8 GB + 데이터)
- [ ] 수집만 하면 GPU 불필요 (학습은 별도 GPU 머신에서)

---

## 1. 시스템 패키지 설치 (apt)

```bash
sudo apt update
sudo apt install -y can-utils ethtool iproute2 ffmpeg git build-essential
```

- `can-utils`, `ethtool`, `iproute2` : CAN 통신
- `ffmpeg` : LeRobot의 카메라 영상 압축 저장
- `git`, `build-essential` : 소스 빌드/클론

---

## 2. Conda 환경 생성 (Python 3.10)

Miniconda가 없다면 먼저 설치한 뒤:

```bash
conda create -n UGRP python=3.10
conda activate UGRP
```

> 이후 모든 단계는 `conda activate UGRP` 상태에서 진행.

---

## 3. LeRobot v0.4.0 설치

```bash
cd ~
git clone https://github.com/huggingface/lerobot.git
cd lerobot
git checkout v0.4.0
pip install -e .
```

> SmolVLA 관련 추가 의존성이 필요하면 repo README 기준으로
> `pip install -e ".[smolvla]"` 형태로 추가 설치. (학습 단계에서만 필요)

---

## 4. UGRP 플러그인 설치

```bash
cd ~
git clone https://github.com/DGIST-JaeminBaek/UGRP.git
cd UGRP
pip install -e .
```

- 이 과정에서 `piper_sdk`, `python-can` 등이 의존성으로 함께 설치됩니다.
  (안 되면 수동으로 `pip install piper_sdk python-can`)

---

## 5. RealSense 카메라 라이브러리

```bash
pip install pyrealsense2
```

- 카메라가 인식 안 되면 udev 규칙/`librealsense` 설치가 필요할 수 있음.
  (Intel 공식 apt 저장소 또는 `sudo apt install librealsense2-utils`)
- 확인용: `realsense-viewer` 로 카메라 영상이 뜨는지 테스트.

---

## 6. CAN 인터페이스 활성화

로봇을 USB로 연결한 뒤:

```bash
sudo ip link set can0 up type can bitrate 1000000
ip link show can0    # <up> 상태면 성공
```

> 파이퍼를 2대 쓰면 CAN 포트가 2개일 수 있음. UGRP repo의
> `find_all_can_port.sh` / dual-arm 문서 참고해 포트 매핑 확인.

---

## 7. 설치 검증

```bash
python -c "import lerobot; print(lerobot.__file__)"
python -c "import lerobot_robot_piper; print('piper plugin OK')"
python -c "from piper_sdk import C_PiperInterface_V2; print('piper_sdk OK')"
```

세 줄 다 오류 없이 출력되면 설치 완료.

카메라 시리얼 확인 (녹화 명령에 넣어야 함):

```bash
python -c "
import pyrealsense2 as rs
ctx = rs.context()
for i, d in enumerate(ctx.devices):
    print(f'[{i}] {d.get_info(rs.camera_info.name)}  serial: {d.get_info(rs.camera_info.serial_number)}')
"
```

→ 출력된 **탑뷰/손목뷰 카메라의 serial 번호**를 메모.

---

## 8. 시연 데이터 녹화 (RealSense 2대)

> ⚠️ 아래 `--robot.top_serial`, `--robot.wrist_serial` 은 **7번에서 확인한
> 내 카메라 시리얼로 교체**. `--dataset.root` 는 **내 노트북 경로로 교체**.

```bash
lerobot-record \
    --robot.type=piper \
    --robot.control_mode=teleop \
    --robot.can_interface=can0 \
    --robot.top_serial=<탑뷰_카메라_시리얼> \
    --robot.wrist_serial=<손목뷰_카메라_시리얼> \
    --teleop.type=piper_slave_only \
    --dataset.repo_id=local/piper-demo \
    --dataset.root=~/datasets/piper-demo \
    --dataset.single_task="pick and place" \
    --dataset.num_episodes=10 \
    --dataset.push_to_hub=false \
    --robot.discover_packages_path=lerobot_robot_piper \
    --teleop.discover_packages_path=lerobot_robot_piper
```

**동작 방식**: 마스터 팔을 손으로 움직이면 → 슬레이브 팔이 CAN으로 따라오고,
그 사이 EEF 상태값 + 카메라 2대 영상이 LeRobotDataset 포맷으로 자동 녹화됨.

> OpenCV 웹캠을 쓸 경우엔 `--robot.top_serial/wrist_serial` 대신
> `--robot.top_index=0 --robot.wrist_index=4` 형태 사용 (repo README 참고).

---

## 9. 수집한 데이터를 서버 U 폴더로 백업

노트북에서 녹화가 끝나면, 서버 U 폴더로 옮겨 보관/학습에 사용:

```bash
# 노트북에서 실행 (서버 접속 정보로 교체)
rsync -avh --progress ~/datasets/piper-demo/ \
    <서버계정>@<서버주소>:/home/p26su01/Kangmook/U/datasets/piper-demo/
```

- 데이터 용량: 640×480·30fps·2대 기준 **시연 100개당 약 1.2 GB** (압축 저장).
- 서버 U 폴더는 용량 여유 충분(399 GB 중 여유 많음).

---

## 참고: 용량 요약

| 항목 | 용량 |
|---|---|
| 노트북 환경 설치 (conda+torch+lerobot+realsense+모델) | 약 5~8 GB (1회성) |
| 시연 데이터 | 100개당 약 1.2 GB (해상도↑/fps↑/depth 저장 시 증가) |
| 노트북 총 권장 여유 | 20~30 GB |

## 참고: repo 내 추가 문서

- `EXPERIMENT.md` — 하드웨어 검증 절차 / 안전 수칙
- `CHANGES.md`, `LINEAGE.md`, `ROADMAP.md` — 변경 이력·설계·계획
- 정확한 옵션/최신 명령은 **UGRP repo README를 항상 우선 확인**할 것.
